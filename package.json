{
  "name": "jntuh-results-backend",
  "version": "1.0.0",
  "description": "JNTUH Results API Server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "docker:build": "docker build -t jntuh-results-api .",
    "docker:run": "docker run -p 5000:5000 -d jntuh-results-api",
    "docker:compose": "docker-compose up -d"
  },
  "dependencies": {
    "axios": "1.6.5",
    "express": "4.18.2",
    "jssoup": "0.0.15"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
